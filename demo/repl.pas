program repl;
uses li;

begin
  writeln('toy lisp interpreter');
  writeln('but really it just parses. so see implish instead:');
  writeln('https://github.com/tangentstorm/implish');
  li.repl;
end.
